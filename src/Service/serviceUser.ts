//import IUser from "../Interface/InterfaceUsers";
//import { SUser } from "../Schema/Schema";

/*class serviceUser{
    async createUser(user: IUser) {
        return await prisma.user.create({
            data: SUser.parse(user)
        })
    }
}*/